const int_balance = 0; // Global variable updated by wallet.js to reflect blanace accross all pages.#login

var x = null;

function init_balance(){

    // Check for each client's UID in a DB File (JSON)
    // If UID is found then it loads the balance amount into the int_balance variable
    // if UID is NOT found (the client is a first-time visitor)
        // Set blanace to some welcome amount 

}

function get_balance(){



}

function set_balance(int){

    // Cheeck for valid math between new and old blanace integers
    // If valid, update otherwise, fix.

}